# Archive

Historical planning notes, phase summaries, and scratch docs. These are kept for reference but **not maintained** — assume any specifics here are stale unless cross-checked against the current code.

Canonical docs live at the repo root: `README.md`, `ARCHITECTURE.md`, `CLAUDE.md`, `DEPLOYMENT.md`, plus per-component READMEs in `aws-backend/` and `admin-ui/`.
